# `hand_teleop` placeholder

Planned owner: Vision, Host, and Arm Control.

This package will calibrate tracked hand motion, map it into the robot frame, apply workspace/rate limits, and publish arm targets. Its default configuration must remain simulation-only.
