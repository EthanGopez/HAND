# `hand_bringup` placeholder

Planned owner: Vision, Host, and Arm Control.

This package will hold reviewed launch files and configurations that connect HAND-owned nodes with the xArm simulation. Physical-arm launch must be explicit and separate from the default path.
