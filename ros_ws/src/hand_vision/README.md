# `hand_vision` placeholder

Planned owner: Vision, Host, and Arm Control.

This package will turn a normal webcam or phone-camera stream into timestamped hand landmarks. The first reference project belongs in `onboarding/vha/hand_tracking_demo/`; promote code here only after its input/output contract is understood.
