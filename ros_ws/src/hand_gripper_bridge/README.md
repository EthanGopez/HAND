# `hand_gripper_bridge` placeholder

Planned owners: VHA and Claw/PCB.

This package will bridge host commands and telemetry to the robot-side claw controller over the agreed transport. The host-to-xArm connection is TCP/IP; the separate claw-controller link must be documented before implementation.
