# `hand_interfaces` placeholder

Planned owner: VHA, reviewed by all subteams.

This package will define the shared ROS messages and services for tracked-hand landmarks, bounded arm targets, gripper commands, force/contact state, EMG intent, and haptic commands. Agree on units, timestamps, coordinate frames, limits, and stale-data behavior in `docs/interfaces/` before generating message files.
